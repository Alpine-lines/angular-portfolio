import { ModifyClassListAtInputBreakpointDirective } from './modify-class-list-at-input-breakpoint.directive';

describe('ModifyClassListAtInputBreakpointDirective', () => {
  it('should create an instance', () => {
    const directive = new ModifyClassListAtInputBreakpointDirective();
    expect(directive).toBeTruthy();
  });
});
