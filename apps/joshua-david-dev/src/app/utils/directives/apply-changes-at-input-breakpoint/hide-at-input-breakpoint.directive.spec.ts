import { HideAtInputBreakpointDirective } from './hide-at-input-breakpoint.directive';

describe('HideAtInputBreakpointDirective', () => {
  it('should create an instance', () => {
    const directive = new HideAtInputBreakpointDirective();
    expect(directive).toBeTruthy();
  });
});
