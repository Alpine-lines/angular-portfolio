import { ShowAtInputBreakpointDirective } from './show-at-input-breakpoint.directive';

describe('ShowAtInputBreakpointDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAtInputBreakpointDirective();
    expect(directive).toBeTruthy();
  });
});
