import { ModifyAttributesAtInputBreakpointDirective } from './modify-attributes-at-input-breakpoint.directive';

describe('ModifyAttributesAtInputBreakpointDirective', () => {
  it('should create an instance', () => {
    const directive = new ModifyAttributesAtInputBreakpointDirective();
    expect(directive).toBeTruthy();
  });
});
